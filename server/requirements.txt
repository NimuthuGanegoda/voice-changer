# # apt-get update
# # apt-get install -y emacs mlocate wget git curl gcc g++ make unzip
# # wget https://repo.anaconda.com/archive/Anaconda3-2022.10-Linux-x86_64.sh
# # bash Anaconda3-2022.10-Linux-x86_64.sh

uvicorn==0.21.1
pyOpenSSL==23.1.1
numpy==1.23.5
torch==2.0.1
torchaudio==2.0.2
resampy==0.4.2
python-socketio==5.8.0
fastapi==0.95.1
python-multipart==0.0.6
# ONNX Runtime: DirectML build on Windows covers AMD/Intel/NVIDIA (DX12) GPUs plus CPU
# in a single wheel. On Linux the CUDA build (falls back to CPU when no GPU); the CUDA 11.8
# build (1.17.1) matches torch 2.0.1. macOS gets the CPU/CoreML build.
onnxruntime-directml==1.17.3; sys_platform == 'win32'
onnxruntime-gpu==1.17.1; sys_platform == 'linux'
onnxruntime==1.17.3; sys_platform == 'darwin'
scipy==1.10.1
matplotlib==3.7.1
websockets==11.0.2
faiss-cpu==1.7.3
torchcrepe==0.0.18
librosa==0.9.1
gin==0.1.6
gin_config==0.5.0
einops==0.6.0
local_attention==1.8.5
websockets==11.0.2
sounddevice==0.4.6
dataclasses_json==0.5.7
onnxsim==0.4.28
torchfcpe==0.0.3
edge-tts
